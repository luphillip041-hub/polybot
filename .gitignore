__pycache__/
*.pyc
.env
.venv/
# runs/ is runtime state (ledger, heartbeat, snapshots). Only the canonical
# allowlist example is tracked; everything else in runs/ stays ignored.
runs/*
!runs/paper/
runs/paper/*
!runs/paper/allowlist.json.example
